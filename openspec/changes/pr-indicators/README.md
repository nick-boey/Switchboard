# pr-indicators

Phase 2: GitHub PR overlay — PR status indicator light (exists/ready/conflicts/checks-failing) + the 'PR exists' filter toggle, via a no-leak GraphQL read path. Depends on branches-control-panel.

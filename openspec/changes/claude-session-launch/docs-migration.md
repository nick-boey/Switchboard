# Documentation migration: claude-session-launch

Ledger of where this change's planning documentation graduates to and what permanent
documentation the change must author. Every row MUST reach `resolved` before archive; every
Plans page linked in `plan.md` MUST have a row.

| Source                                                                                                                                                                       | Destination                                            | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Status |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `docs/plans/switchboard/mvp.md` — the programme page (linked in `plan.md`; lists this change in its `openspec-changes` frontmatter)                                           | — (page persists while the sibling `runtime-cli-docker` stays active) | `retire — trim` at archive: drop the `claude-session-launch` entry from the `openspec-changes` frontmatter and trim prose specific to this change. The **path-safe naming scheme reused for tmux session names** and the **session-existence + worktree-mapping-only** decision are cross-cutting "direction" records the page keeps unchanged; the full design now lives in this change's `design.md`/specs — no shared decision is altered, so the page's locked decisions carry over for `runtime-cli-docker`                                                                                                              | open   |
| `plan.md` "Planned architecture" — the deferred overlay scaffold for this change (realizes the base model's `#planned` `Switchboard.Api -> TmuxHost`; introduces the Session service inside `Switchboard.Api`) | `docs/dev/Architecture/Planned/claude-session-launch.c4` | `author → docs/dev/Architecture/Planned/claude-session-launch.c4`: a new LikeC4 overlay authored during implementation — `extend` `Switchboard.Api` with the **Session service** (session launch/stop/list, the `session`-typed operation-ledger usage, and the tmux-backed liveness probe wired into the worktree slice's safe-to-delete seam) and realize the `Switchboard.Api -> TmuxHost` relationship, every addition tagged `#todo`, view ids prefixed `claude-session-launch-*`, validated with `pnpm --dir site exec likec4 validate --no-layout ../docs/dev/Architecture`. The Architecture review checkpoint fires when it lands | open   |

No `docs/user` page is authored by this change: session launch/stop/listing is surfaced through the
web UI (the per-worktree plug on the worktrees hub — there is no standalone session screen), and
host/runtime/user documentation (tmux + the `claude` login the session rides) lands with the CLI in
`runtime-cli-docker`. The tmux naming reuse and the session-liveness seam fulfilment are documented in
`design.md` and proven by the spec's scenarios (in-code/tests, not a `docs/dev` prose page).

The prototype-disposition for `apps/web/src/prototypes/ui-prototypes-mvp/worktrees.stories.tsx` (this
change consumes its per-worktree **plug**/session model into production by wiring the plug's on/off
behaviour) is tracked in `ui-prototypes-mvp`'s `prototypes.md` ledger and reconciled by the archive
skill, not here.

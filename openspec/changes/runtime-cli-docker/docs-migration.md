# Documentation migration: runtime-cli-docker

Ledger of where this change's planning documentation graduates to and what permanent
documentation the change must author. Every row MUST reach `resolved` before archive; every
Plans page linked in `plan.md` MUST have a row.

| Source                                                                                                                              | Destination                                              | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status |
| ----------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `docs/plans/switchboard/mvp.md` — the programme page (linked in `plan.md`; lists this change in its `openspec-changes` frontmatter) | —                                                        | `retire — close out`: this is the **last** MVP change. At archive, drop the `runtime-cli-docker` entry from the `openspec-changes` frontmatter (leaving it empty — no active changes remain) and trim prose specific to this change. The page persists as the durable **completed-MVP** programme record (its locked cross-cutting decisions, security posture, and Future-features list outlive the MVP); no shared decision is altered — the serve-ingress hardening it already names as "the deferred hardening (`runtime-cli-docker`)" is now realised (as a dedicated, non-host-published loopback-TCP serve ingress, since `tailscale serve` has no UDS target — see `design.md` Decision 3), and the full design lives in this change's `design.md`/specs | open   |
| `docs/dev/spikes/runtime-spike.md` + the throwaway `spikes/runtime/` harness (referenced by `plan.md`)                              | —                                                        | `retire — consume then delete`: `foundations` deferred the spike doc's retirement to this change ("which retires the spike doc when it consumes it"). This change consumes the spike findings (userspace Tailscale, serve→loopback identity, config/state persistence, the Claude-credential follow-up, the pinned `serve` invocation) into `design.md` + the `container-runtime`/`cli-runtime` specs; at archive, delete the throwaway `spikes/runtime/` harness directory and retire the findings doc (its durable conclusions now live in this change's artifacts and the runtime guide)             | open   |
| `plan.md` "Planned architecture" — the deferred overlay scaffold for this change                                                    | `docs/dev/Architecture/Planned/runtime-cli-docker.c4`    | `author → docs/dev/Architecture/Planned/runtime-cli-docker.c4`: a LikeC4 overlay authored during implementation — `extend` `Switchboard.Cli` with its orchestration/supervision role (config bootstrap, supervised `start(ctx)`, `--docker` bring-up of `tailscaled` + `tailscale serve`) and realise the `Tailscale -> Switchboard.Api` serve ingress over the **dedicated loopback serve port**; tag every addition `#todo`; prefix view ids `runtime-cli-docker-*`; list the added element/view ids in `plan.md`; validate with `pnpm --dir site exec likec4 validate --no-layout ../docs/dev/Architecture`. The Architecture review checkpoint fires when it lands | open   |
| — (design Decisions 6–7; the host/runtime/user docs `claude-session-launch` explicitly deferred here)                               | `docs/user/running-switchboard.md`                       | `author → docs/user/running-switchboard.md`: the user-facing runtime/deployment guide — npm install + local `switchboard start`; the Docker run (image, named volumes for tailscale state / `~/.switchboard` / `~/.claude`, mounted PAT + Tailscale auth-key secrets, userspace vs kernel-TUN); the Tailscale prerequisites (auth key, MagicDNS + HTTPS certs for `tailscale serve`); the **in-container `claude` login** + `~/.claude` volume credential strategy (macOS keychain caveat); and the dedicated serve-ingress / bearer-loopback access model                                  | open   |
| `README.md` (install/build/run-locally section authored by `foundations`)                                                          | `README.md`                                              | `merge → README.md`: add the **npm distribution** install path (`npx switchboard` / `npm i -g`) and a "run on the tailnet (Docker)" pointer to `docs/user/running-switchboard.md`, so the headline install/run story reflects the shipped runtime                                                                                                                                                                                                                                                                                                                       | open   |

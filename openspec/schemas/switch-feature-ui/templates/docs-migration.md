# Documentation migration: {{change-name}}

Ledger of where this change's planning documentation graduates to and what permanent
documentation the change must author. Every row MUST reach `resolved` before archive;
every Plans page linked in plan.md MUST have a row.

<!--
Action vocabulary (exactly one per row):
  author → <path>     New permanent page, written DURING IMPLEMENTATION (and listed as
                      a tasks.md task); the archive verifies it exists.
  merge → <path>      Content folded into an existing page during implementation.
  retire — <how>      Plans page deleted (or trimmed, when other active changes still
                      list it) at archive.
  discard — <reason>  Planning content that is deliberately ephemeral.

If the change has no documentation impact, DELETE the table and state:
  No documentation impact — <reason>.
-->

| Source | Destination | Action | Status |
| --- | --- | --- | --- |

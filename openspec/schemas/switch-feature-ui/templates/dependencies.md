---
depends-on: []
---

<!--
List the names of other OpenSpec changes that must complete before this change can be
implemented:

depends-on:
  - other-change-name

A dependency is satisfied when the named change is archived (a folder under
openspec/changes/archive/) or all of its tasks are complete. An empty list is valid
and common. Record ordering constraints HERE — never as prose in tasks.md.
Optional notes on why each dependency exists go below this comment.
-->
